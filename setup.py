from distutils.core import setup

setup(name='anvil',
      version='0.2',
      description='Anvil works with Kiln to provide programmatic access to data',
      packages=['anvil'],
      install_requires=['requests'],
)
